# =============================================
# DIJITAL ANIT - APPLICATION CONFIGURATION
# =============================================

spring.application.name=dijital-anit

# ==================== DATABASE ====================
spring.datasource.url=jdbc:postgresql://localhost:5432/postgres
spring.jpa.properties.hibernate.default_schema=public
spring.datasource.username=postgres
spring.datasource.password=308233

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# ==================== FILE UPLOAD ====================
# Python: app.config['UPLOAD_FOLDER'] karşılığı
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=100MB
app.upload.dir=uploads
app.upload.images-dir=${app.upload.dir}/images
app.upload.audio-dir=${app.upload.dir}/audio
app.upload.video-dir=${app.upload.dir}/video

# ==================== JWT ====================
app.jwt.secret-key=JfVoLB61pmyKbWswGsuxD6TMnPFZzsoOACmSd1mA5hM=
app.jwt.access-token-expiration=7200000
app.jwt.refresh-token-expiration=14400000

# ==================== MAIL ====================
# Python: app.config['MAIL_SERVER'] karşılığı
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=dijitalanit.info@gmail.com
spring.mail.password=rqnrhvgvqevzxwym
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
app.mail.from=dijitalanit.info@gmail.com

# Mail konfigürasyonu opsiyonel — yoksa uygulama yine de başlasın
# spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.mail.MailSenderAutoConfiguration

app.frontend.url=http://localhost:5173

# ==================== SERVER ====================
server.error.include-message=always
server.error.include-binding-errors=always

# ==================== IMAGE OPTIMIZATION ====================
# Python: optimize_image(max_width=1200) karşılığı
app.image.max-width=1200
app.image.profile-max-width=800
app.image.quality=85
